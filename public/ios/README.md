Add HEVC-with-alpha .mov files here for smooth iOS playback:
- idle.hevc.mov
- listening.hevc.mov
- impressed.hevc.mov
- disappointed.hevc.mov

These are optional. If missing or unsupported, the app falls back to WebM with a canvas chroma-key.
